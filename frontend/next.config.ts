import type { NextConfig } from "next";

const ACTIVE_TUNNEL_URL = "https://witness-realistic-plane-tue.trycloudflare.com";

const nextConfig: NextConfig = {
  async rewrites() {
    return [
      {
        source: "/api/:path*",
        destination: `${ACTIVE_TUNNEL_URL}/api/:path*`,
      },
    ];
  },
};

export default nextConfig;
